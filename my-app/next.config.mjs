/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
    domains: ["sparktraineeships.com", "sparkproductshowcase.lovable.app"],
  },
};

export default nextConfig;
